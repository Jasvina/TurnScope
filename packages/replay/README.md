# Replay

Future home for session reconstruction and replay helpers.

The first replay goal is modest:

- reopen a stored session
- reconstruct turns and derived panels
- make debugging possible without the original runtime running
