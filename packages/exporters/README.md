# Exporters

Future home for format exporters.

Likely first targets:

- portable TurnScope JSON bundle
- OpenTelemetry-compatible trace bundle
- OpenInference-friendly export

Exporter rule:

- exporting should never be the only way to understand a session locally
