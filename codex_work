本文件 `codex_work` 是 TurnScope 仓库的连续工作记录。后续无论是哪一个 Codex、AI 代理，凡是在本仓库内开展工作，都应把“计划 + 执行 + 结果 + 验证 + 风险 + 下一步计划”优先追加到这个文件里，而不是只留在聊天上下文中。目标是让一个完全没有历史上下文的新 AI，仅通过阅读本文件，就能快速理解：这个项目为什么存在、已经做了什么、当前卡在哪里、下一步最应该做什么。

建议后续记录格式保持稳定：
- 时间：xx月xx日xx时xx分
- 工作类型：研究 / 文档 / 架构 / 实现 / 验证 / 修复
- 计划：
- 执行：
- 结果：
- 验证：
- 风险 / 未决问题：
- 下一步计划：

================================================================

时间：04月20日23时35分
工作类型：项目研究 + README 首版撰写 + 仓库工作流约束落盘

计划：
- 调研 TurnScope 这个方向是否值得做，明确它与 Codex、OpenClaw、LLM tracing 平台的差异化定位。
- 在本地仓库里产出一版适合开源首发的 README，让项目价值、范围、路线图和贡献入口足够清晰。
- 为后续 AI 协作建立一个持久工作记录文件，避免上下文丢失。

执行：
- 对 coding-agent 可视化 / 可观测性方向做了产品级定位思考，结论是：TurnScope 最佳定位不是“再做一个 agent IDE”，而是“面向 coding agents 的 local-first observability + replay layer”。
- 收敛出核心产品定义：统一展示 session timeline、tool calls、shell process tree、file diff timeline、approval lane、subagent graph、model/cost lane、replay mode。
- 在 `/Users/jasvina/jasvina_github/TurnScope/README.md` 写了首版 README，采用“项目宣言 + 架构图 + 里程碑 + 路线图”的结构。
- 新增两张 README 配图：
  - `docs/assets/turnscope-hero.svg`
  - `docs/assets/turnscope-panels.svg`
- README 中明确了：
  - 项目一句话定位：`Local-first observability and replay for coding agents.`
  - v0.1 目标范围
  - 初版事件类型
  - 建议的 monorepo 目录结构
  - 贡献者最值得切入的方向
- 根据用户新要求，在仓库根目录创建本文件 `codex_work`，用于持续记录所有计划、执行、结果与下一步。

结果：
- TurnScope 已经有一版可直接放到 GitHub 首页展示的 README 初稿，不再只是一个空仓库名字。
- 项目的差异化叙事已经比较清晰：不是与 Codex/OpenClaw 正面竞争运行器，而是补“运行过程可视化、可追踪、可回放”的透明度层。
- 当前仓库里已具备最基础的“对外叙事资产”：
  - `README.md`
  - `docs/assets/turnscope-hero.svg`
  - `docs/assets/turnscope-panels.svg`
  - `codex_work`

验证：
- 已检查 `README.md` 存在且内容完整。
- 已检查两张 SVG 文件存在，README 中引用路径有效。
- 已确认当前本地工作区路径为：`/Users/jasvina/jasvina_github/TurnScope`

风险 / 未决问题：
- 当前本地仓库处于 `No commits yet on main` 状态，说明本地 checkout 没有成功拿到一个可验证的远端提交历史；后续在正式提交前，需要先确认本地与 GitHub 远端仓库状态一致。
- 目前只有文档和示意图，尚未开始代码骨架与事件 schema 的实际实现。
- README 里描述的是清晰方向，但还没有配套的 `LICENSE`、`CONTRIBUTING.md`、issue templates、架构文档、代码目录骨架去承接外部贡献。

下一步计划：
- 优先补一份 `docs/specs/v0.1-architecture.md`，把 collector、schema、adapters、web UI 的边界先定下来。
- 同步搭第一版代码骨架，建议从这些目录开始：
  - `apps/web`
  - `apps/collector`
  - `packages/schema`
  - `packages/adapters-codex`
  - `packages/adapters-openclaw`
- 补开源协作基础设施：
  - `LICENSE`
  - `CONTRIBUTING.md`
  - `.github/ISSUE_TEMPLATE/`
  - `docs/specs/`
- 在每次后续编辑结束后，继续把变更摘要、验证结论和下一步写回本文件，保证新 AI 接手时不依赖对话历史。

================================================================

时间：04月20日23时39分
工作类型：发布准备 + GitHub 上传

计划：
- 检查本地仓库与远端仓库状态，确认能否直接提交并推送。
- 把当前 README、配图和 `codex_work` 一并纳入版本控制。
- 按 Lore commit protocol 创建首个有意义的提交，并上传到 GitHub。

执行：
- 检查了 `/Users/jasvina/jasvina_github/TurnScope` 的 git 状态，确认当前待提交文件为：
  - `README.md`
  - `codex_work`
  - `docs/assets/turnscope-hero.svg`
  - `docs/assets/turnscope-panels.svg`
- 检查了当前分支为 `main`。
- 检查远端状态，发现本地仓库尚未配置 `origin`，且 GitHub 远端当前可视为未同步内容，需要由本地首次推送建立可见项目内容。
- 将本��“上传 GitHub”的计划与执行记录追加到本文件，确保后续 AI 接手时能看到发布脉络。

结果：
- 当前仓库已处于可提交状态。
- 下一步就是配置远端、创建 Lore 格式提交并执行首次推送。

验证：
- 已核对本地待提交文件列表。
- 已核对当前分支名称为 `main`。

风险 / 未决问题：
- 若远端仓库已有隐藏或未同步的提交，推送时可能需要先拉取或人工处理分支关系。
- 当前尚未验证 GitHub 认证链路是否可直接推送成功。

下一步计划：
- 配置 `origin` 指向 `https://github.com/Jasvina/TurnScope.git`
- 创建符合 Lore protocol 的提交
- 推送 `main` 到 GitHub 并验证远端可见

================================================================

时间：04月21日00时00分
工作类型：GitHub 发布完成 + 记录补全

计划：
- 完成首个 Lore 提交并推送到 GitHub。
- 验证本地分支已经与远端 `origin/main` 建立跟踪关系。
- 把“上传成功”的结果补写回 `codex_work`，保持仓库内记录闭环。

执行：
- 创建了首个 Lore protocol 提交，提交标题为：
  - `Make TurnScope's launch thesis clear to early contributors`
- 提交包含文件：
  - `README.md`
  - `codex_work`
  - `docs/assets/turnscope-hero.svg`
  - `docs/assets/turnscope-panels.svg`
- 将远端地址设置为：
  - `git@github.com:Jasvina/TurnScope.git`
- 初次使用 HTTPS 推送时遇到本机凭证读取失败，随后检查认证链路，确认：
  - `gh auth status` 显示 GitHub 账号 `Jasvina` 已登录
  - SSH 到 GitHub 认证成功
- 之后改用 SSH 远端并成功执行：
  - `git push -u origin main`

结果：
- TurnScope 的首版 README、配图和工作记录已经成功上传到 GitHub。
- 当前本地分支 `main` 已与 `origin/main` 建立跟踪关系。
- 当前首个有效提交短哈希为：
  - `239f5ce`

验证：
- 推送命令返回成功，显示：
  - `[new branch] main -> main`
  - `branch 'main' set up to track 'origin/main'`
- 本地状态显示：
  - `## main...origin/main`

风险 / 未决问题：
- 本次验证主要基于 git 推送成功回执与本地跟踪状态，尚未额外检查 GitHub 网页端的最终展示效果。
- README 虽然已经上线，但后续仍需要补更多承接贡献者的仓库基础设施。

下一步计划：
- 补 `docs/specs/v0.1-architecture.md`
- 搭第一版代码骨架
- 补开源基础文件（如 `LICENSE`、`CONTRIBUTING.md`、issue templates）
- 每完成一轮工作后，继续同步更新本文件

================================================================

时间：04月21日00时18分
工作类型：架构落地 + 代码骨架 + 开源基础设施 + 验证

计划：
- 把上一个阶段的“方向”和 README 宣言，推进到可执行的仓库骨架。
- 补一份能指导后续实现的 v0.1 架构文档。
- 产出最小但真实可用的 collector、schema、web prototype 和开源协作文件。
- 验证这些新文件至少能在本地通过静态和脚本级检查。

执行：
- 新增架构文档：
  - `docs/specs/v0.1-architecture.md`
  - 内容覆盖：目标、非目标、组件边界、canonical event envelope、事件 taxonomy、存储策略、数据流、v0.1 验收标准、开放问题。
- 新增 schema 包：
  - `packages/schema/README.md`
  - `packages/schema/turnscope-event.schema.json`
  - `packages/schema/examples/minimal-session.ndjson`
  - `packages/schema/examples/subagent-branch.ndjson`
- 新增 runtime adapter 占位说明：
  - `packages/adapters-codex/README.md`
  - `packages/adapters-openclaw/README.md`
- 新增 replay / exporter 占位包说明：
  - `packages/exporters/README.md`
  - `packages/replay/README.md`
- 新增 collector 原型：
  - `apps/collector/README.md`
  - `apps/collector/src/collector.py`
  - 功能：读取 NDJSON 或 JSON array，验证最小字段，按 `session_id` 写入 append-only session log，并生成 summary sidecar。
- 新增 web 原型：
  - `apps/web/README.md`
  - `apps/web/index.html`
  - `apps/web/styles.css`
  - `apps/web/app.js`
  - 特点：零依赖静态页面、可直接粘贴 NDJSON、可在浏览器中派生 timeline / shell tree / diff / subagent graph 视图。
- 新增开源协作基础设施：
  - `LICENSE`
  - `CONTRIBUTING.md`
  - `.github/ISSUE_TEMPLATE/bug-report.yml`
  - `.github/ISSUE_TEMPLATE/feature-request.yml`
  - `.github/ISSUE_TEMPLATE/runtime-adapter-request.yml`
  - `.github/ISSUE_TEMPLATE/config.yml`
  - `.gitignore`
- 更新 `README.md`：
  - 加入 MIT license badge
  - 加入 “Prototype today” 章节
  - 把 roadmap 的一部分状态从纯设想更新为已落地 bootstrap
  - 将仓库从“纯 thesis”更新为“有小型 working scaffold 的 pre-alpha 项目”

结果：
- TurnScope 已经不再只是一个概念仓库，而是进入了可继续迭代的 bootstrap 阶段。
- 现在仓库已经同时具备：
  - 对外叙事层：`README.md`
  - 架构层：`docs/specs/v0.1-architecture.md`
  - 契约层：`packages/schema/turnscope-event.schema.json`
  - 数据层样例：`packages/schema/examples/*.ndjson`
  - 本地采集层：`apps/collector/src/collector.py`
  - 前端原型层：`apps/web/*`
  - 开源协作层：`LICENSE`、`CONTRIBUTING.md`、issue templates
- 后续任何新 AI 接手，都能在仓库内部直接找到“为什么做、怎么做、当前做到哪、下一步做什么”。

验证：
- 运行 collector 成功：
  - 输入：`packages/schema/examples/minimal-session.ndjson`
  - 输出：
    - `apps/collector/data/sessions/sess_demo.ndjson`
    - `apps/collector/data/sessions/sess_demo.summary.json`
- 校验通过：
  - `packages/schema/turnscope-event.schema.json` 可被 JSON 成功解析
  - 两个 sample NDJSON 文件逐行 JSON 解析成功
  - `python3 -m py_compile apps/collector/src/collector.py` 通过
  - `node --check apps/web/app.js` 通过
- 附加检查：
  - `README.md` 已提及关键新文件：architecture doc、schema、collector、`CONTRIBUTING.md`、`codex_work`
- 附加说明：
  - 当前 sandbox 不允许绑定本地 HTTP 端口，因此未能在此环境完成浏览器级端口验证

风险 / 未决问题：
- `apps/web` 目前仍是静态 prototype，还没有真正读取 collector 输出的本地 API 或文件索引。
- adapter 目录当前还是 contract / mapping 占位，尚未开始真正对接 Codex 或 OpenClaw 的事件源。
- schema 目前是 v0.1 草案，还未经历真实 runtime trace 的逆向校准。

下一步计划：
- 实现第一个真实 adapter，优先建议 `packages/adapters-codex`
- 让 `apps/web` 读取本地 collector 输出，而不是只靠内嵌 sample 数据
- 为 `apps/collector` 增加更清晰的 session index 文件与 dropped-event 统计
- 补一份 event mapping 文档，把 runtime-native event names 对齐到 canonical types
- 如需增强开源承接能力，可继续补：
  - `.github/pull_request_template.md`
  - `SECURITY.md`
  - `docs/specs/adapter-mapping-codex.md`

================================================================

时间：04月21日00时22分
工作类型：提交与发布同步

计划：
- 把本轮架构、骨架和协作基础设施提交到仓库历史。
- 将最新变更推送到 GitHub，确保远端与本地一致。
- 记录本次提交和推送结果，避免后续 AI 误判“改动只在本地”。

执行：
- 创建 Lore protocol 提交：
  - `Turn TurnScope into a buildable bootstrap instead of a pure thesis`
- 本次提交包含：
  - v0.1 架构文档
  - schema 与 sample traces
  - collector 原型
  - web prototype
  - LICENSE / CONTRIBUTING / issue templates / .gitignore
  - README 更新
  - 本文件 `codex_work` 更新
- 成功推送到 GitHub：
  - `origin/main`

结果：
- 当前仓库已经把“概念 README 仓库”推进为“有架构、有 contract、有原型、有开源协作面的 bootstrap 仓库”。
- 最新���交短哈希为：
  - `6b62d77`

验证：
- 推送成功回执：
  - `e48069a..6b62d77  main -> main`
- 当前本地状态：
  - `## main...origin/main`

风险 / 未决问题：
- Git 历史中的提交者名字和邮箱仍是本机自动推断值，若后续需要更规范的作者信息，可再配置 `git config user.name` 与 `git config user.email`。
- 真实 adapter 与 UI-collector 联通尚未开始，当前 repo 仍处在 bootstrap 早期。

下一步计划：
- 进入第一个真实 runtime adapter 开发阶段
- 优先打通 collector 输出到 web 的本地读取链路
- 按真实 trace 迭代 schema

================================================================

时间：04月21日09时28分
工作类型：Codex adapter 实装 + collector index + web 文件加载

计划：
- 把下一阶段从“静态骨架”推进到“有真实 runtime 对接能力的 bootstrap”。
- 基于官方 Codex app-server 文档做首版真实事件映射。
- 让 collector 生成 `index.json`，并让 web prototype 能读取 collector 输出文件。

执行：
- 查阅官方 Codex app-server README，抽取第一批稳定可映射事件面：
  - turn lifecycle
  - commandExecution
  - fileChange
  - approval requests
  - `serverRequest/resolved`
  - `collabToolCall` 的 `spawn_agent`
- 新增 Codex adapter 设计文档：
  - `docs/specs/adapter-mapping-codex.md`
- 将 `packages/adapters-codex` 从占位说明升级为可运行包：
  - `packages/adapters-codex/README.md`
  - `packages/adapters-codex/fixtures/sample-app-server.jsonl`
  - `packages/adapters-codex/src/map_app_server.py`
- adapter 当前支持把 Codex app-server JSONL 转成 TurnScope canonical NDJSON，覆盖：
  - `turn.started`
  - `turn.finished`
  - `shell.started`
  - `shell.output`
  - `shell.finished`
  - `file.changed`
  - `approval.requested`
  - `approval.resolved`
  - `subagent.spawned`
  - `error.raised`
- 增强 collector：
  - 重写 `apps/collector/src/collector.py`
  - 在每次 ingest 后额外生成 `apps/collector/data/index.json`
  - index 中保留 `log_path` 与 `summary_path`，方便 web prototype 或后续 API 读取
- 增强 web prototype：
  - 更新 `apps/web/index.html`
  - 更新 `apps/web/styles.css`
  - 更新 `apps/web/app.js`
  - 更新 `apps/web/README.md`
- web 现在支持三类输入：
  - 内置 sample trace
  - 粘贴 NDJSON
  - 上传 collector 输出文件：`index.json`、`*.summary.json`、`*.ndjson`
- 同步更新 `README.md`：
  - 加入 Codex adapter 路径说明
  - 加入 adapter -> collector -> web 的新 quickstart
  - 将 roadmap 中“首个 runtime adapter”标记为已完成 bootstrap 版本

结果：
- TurnScope 现在已经有了第一个“真实而不是占位”的 runtime adapter：Codex app-server adapter。
- collector 不再只会写 session 文件，还能生成 session catalog，这让 web prototype 开始具备读取真实输出的可能。
- web prototype 不再只能看内置样例，而是能接住 collector 产物，形成更完整的本地工作流。

验证：
- Codex adapter 运行成功：
  - 输入：`packages/adapters-codex/fixtures/sample-app-server.jsonl`
  - 输出：`/tmp/turnscope_codex_adapter.ndjson`
  - 结果：10 条 Codex message 转为 9 条 TurnScope event
- collector 运行成功：
  - 输入：`/tmp/turnscope_codex_adapter.ndjson`
  - 输出：
    - `apps/collector/data/sessions/thr_demo.ndjson`
    - `apps/collector/data/sessions/thr_demo.summary.json`
    - `apps/collector/data/index.json`
- 语法检查通过：
  - `python3 -m py_compile apps/collector/src/collector.py`
  - `python3 -m py_compile packages/adapters-codex/src/map_app_server.py`
  - `node --check apps/web/app.js`

风险 / 未决问题：
- 当前 Codex adapter 仍是 bootstrap 映射，不包含 token usage、plan updates、diff streaming、model reroute 等更复杂信号。
- `serverRequest/resolved` 只能表示审批请求已结束，不一定完全表达最终用户决定语义；更细粒度信息还要结合对应 item 完成事件继续打磨。
- web prototype 已能加载文件，但还没有自动发现本地 `apps/collector/data/index.json` 的能力；仍需用户显式上传或粘贴。

下一步计划：
- 继续丰富 Codex adapter 覆盖面
- 开始第一个 OpenClaw adapter
- 为 web 增加 session picker 与更细的 raw event inspector
- 让 collector 支持多 session 连续 ingest 和更清晰的 dropped-event 统计

================================================================

时间：04月21日09时38分
工作类型：真实样例驱动的精度优化

计划：
- 把 Codex adapter 从“能跑”推进到“可被样例回归约束”。
- 引入 fixture + golden output + precision eval 的工作流。
- 让后续精度优化不是凭感觉改映射，而是有样例、有期望输出、有已知 gap 清单。

执行：
- 增强 `packages/adapters-codex/src/map_app_server.py`：
  - 增加 `session.started` 与 `session.finished` 的合成逻辑
  - 增加 `--report` 输出能力
  - 增加按 `method + item.type` 维度记录 mapped / unmapped method 的精细化统计
- 新增第二组更贴近真实任务形态的 fixture：
  - `packages/adapters-codex/fixtures/tool-and-error-app-server.jsonl`
- 保留并继续使用首个审批 / diff / spawn fixture：
  - `packages/adapters-codex/fixtures/sample-app-server.jsonl`
- 新增 golden outputs：
  - `packages/adapters-codex/golden/sample-app-server.expected.ndjson`
  - `packages/adapters-codex/golden/tool-and-error-app-server.expected.ndjson`
- 新增 precision eval 脚本：
  - `packages/adapters-codex/src/eval_samples.py`
  - 功能：对每个 fixture 运行 adapter，和 golden NDJSON 做逐事件比对，若不一致则失败
- 新增已知 gap allowlist：
  - `packages/adapters-codex/expected_gaps.json`
  - 当前显式允许的 gap：
    - `item/started:fileChange`
- 更新文档：
  - `packages/adapters-codex/README.md`
  - `README.md`
  - 将 adapter 的 quickstart 和 precision eval 命令写回仓库对外文档

结果：
- Codex adapter 已经进入真正的“样例驱动精度优化”阶段，而不是只靠人工 spot-check。
- 现在任何对 adapter 的修改，都可以被 fixture + golden 输出回归约束。
- unmapped method 也不再是模糊统计，而是具体到 `item/started:fileChange` 这一类颗粒度，便于后续定点补齐。

验证：
- `python3 packages/adapters-codex/src/eval_samples.py` 通过：
  - `sample-app-server.jsonl -> 11 events`
  - `tool-and-error-app-server.jsonl -> 9 events`
- precision report 输出正常，当前可见已知 unmapped：
  - `item/started:fileChange`
- 继续验证 adapter -> collector 链路：
  - adapter 输出 11 条 TurnScope event
  - collector 成功写出：
    - `apps/collector/data/sessions/thr_demo.ndjson`
    - `apps/collector/data/sessions/thr_demo.summary.json`
    - `apps/collector/data/index.json`
- 语法检查继续通过：
  - `python3 -m py_compile packages/adapters-codex/src/map_app_server.py`
  - `python3 -m py_compile packages/adapters-codex/src/eval_samples.py`
  - `python3 -m py_compile apps/collector/src/collector.py`
  - `node --check apps/web/app.js`

风险 / 未决问题：
- 当前 golden 仍然来自 repo 内 committed fixture，而不是来自真实用户采样回放；要进一步逼近真实世界，还需要后续接入更多真实 trace 样例。
- `item/started:fileChange` 目前被纳入 allowlist，说明这块还没有 canonical event 对应策略，未来要决定是新增事件、映射到已有事件，还是明确保持忽略。
- 目前精度评测集中在 Codex adapter，OpenClaw 还没有进入同样的 fixture-driven 阶段。

下一步计划：
- 收集更多 Codex app-server 样例并扩展 golden 集
- 决定 `item/started:fileChange` 的 canonical 表达方式
- 开始 OpenClaw adapter 的首版 fixture 和 mapping
- 如本地端口权限获批，则补做 web prototype 的浏览器级验证

================================================================

时间：04月21日09时45分
工作类型：OpenClaw bootstrap adapter + 真实样例验证

计划：
- 沿着和 Codex 相同的方法，把 OpenClaw 也推进到“样例驱动、可验证”的 bootstrap adapter 状态。
- 优先选择官方文档里最稳定、最明确的数据面，而不是猜测未文档化的内部事件流。
- 用 fixture + golden 输出把 OpenClaw 这条链路也纳入可回归状态。

执行：
- 基于官方文档整理 OpenClaw 的 bootstrap mapping：
  - `docs/specs/adapter-mapping-openclaw.md`
  - 明确当前最稳定的输入面是 gateway 的 session store：
    - `~/.openclaw/agents/<agentId>/sessions/sessions.json`
- 将 `packages/adapters-openclaw` 从占位说明升级为可运行包：
  - `packages/adapters-openclaw/README.md`
  - `packages/adapters-openclaw/fixtures/sample-sessions-store.json`
  - `packages/adapters-openclaw/src/map_session_store.py`
  - `packages/adapters-openclaw/src/eval_samples.py`
  - `packages/adapters-openclaw/golden/sample-sessions-store.expected.ndjson`
- 当前 OpenClaw adapter 的策略不是伪装成完整 turn-level event stream，而是明确声明为“session-store snapshot adapter”：
  - 每个 session store entry 合成为 `session.started`
  - 每个 session store entry 合成为 `session.finished`
  - `session.finished.payload.status` 使用 `active_snapshot`，避免错误暗示“真实运行已结束”
- 在实现中修复了一个排序精度问题：
  - 对于没有 `createdAt`、只有 `updatedAt` 的 session，开始/结束事件时间相同
  - 如果只按 `(occurred_at, id)` 排序，会出现 `session.finished` 排在 `session.started` 之前
  - 现已在 `map_session_store.py` 中增加 type-order 排序，确保 `session.started` 永远先于 `session.finished`
- 同步更新 `README.md`：
  - 加入 OpenClaw adapter 的路径和 quickstart
  - 将 OpenClaw bootstrap adapter 标记为已完成

结果：
- TurnScope 现在已经具备两条可验证的 runtime adapter 路径：
  - Codex app-server adapter
  - OpenClaw session-store bootstrap adapter
- OpenClaw 这条链路虽然还不是完整事件流，但已经能把官方文档支持的稳定 session snapshot 数据，转换成 collector 可消费的 canonical event。
- 项目从“单 runtime bootstrap”变成了“跨两个 runtime 的真实 bootstrap”。

验证：
- `python3 packages/adapters-openclaw/src/eval_samples.py` 通过：
  - `sample-sessions-store.json -> 4 events`
- adapter -> collector 链路通过：
  - OpenClaw adapter 输出 4 条 TurnScope event
  - collector 成功写出：
    - `apps/collector/data/sessions/oc_sess_main_001.ndjson`
    - `apps/collector/data/sessions/oc_sess_main_001.summary.json`
    - `apps/collector/data/sessions/oc_sess_cron_001.ndjson`
    - `apps/collector/data/sessions/oc_sess_cron_001.summary.json`
    - `apps/collector/data/index.json`
- 语法检查通过：
  - `python3 -m py_compile packages/adapters-openclaw/src/map_session_store.py`
  - `python3 -m py_compile packages/adapters-openclaw/src/eval_samples.py`

风险 / 未决问题：
- 当前 OpenClaw adapter 只覆盖 session-store snapshot，不覆盖 transcript JSONL、gateway logs、turn-level execution 或 tool-level行为。
- `active_snapshot` 是一个谨慎但仍然合成的状态语义，后续如果拿到更稳定的 transcript / run-complete 信号，可能需要重新建模。
- OpenClaw 的更细粒度事件流目前仍需要更多真实样例支撑，不能只靠文档猜。

下一步计划：
- 继续扩大 Codex fixture 集，尤其是更多 tool / approval / subagent 场景
- 为 OpenClaw 补第二条输入面，优先考虑 gateway log JSON line 或 transcript JSONL 的真实样例
- 如果本地端口权限获批，补做 web prototype 的浏览器级真实验证

================================================================

时间：04月21日09时58分
工作类型：collector bundle 导出 + web 单文件加载链路

计划：
- 把 collector 和 web 之间的真实文件链路再推进一步。
- 解决当前“必须分别加载 index.json 和 ndjson”带来的使用摩擦。
- 产出一个适合本地查看和 handoff 的单文件 session bundle 格式。

执行：
- 增强 `apps/collector/src/collector.py`：
  - 在写 `sessions/*.ndjson`、`sessions/*.summary.json`、`index.json` 之外
  - 额外写出 `bundles/<session_id>.bundle.json`
- 当前 bundle 结构为：
  - `version`
  - `kind = turnscope.session.bundle`
  - `summary`
  - `events`
- 同步把 `bundle_path` 写回 summary 和 index，方便后续读取
- 增强 web 原型：
  - 更新 `apps/web/app.js`
  - 现在除了 `index.json`、`*.summary.json`、`*.ndjson` 外，也支持直接加载 `*.bundle.json`
  - 加载 bundle 后，web 会同时恢复：
    - session summary
    - collector catalog 卡片
    - timeline / shell / diff / subagent graph
- 更新文档：
  - `apps/collector/README.md`
  - `apps/web/README.md`
  - `README.md`
  - 补充 bundle 的用途与工作流说明

结果：
- TurnScope 现在已经有了更实用的单文件 handoff 格式。
- 这意味着后续无论是本地验证、给别人复现、还是让 web prototype 离线读一个 session，都不再依赖多文件拼装。
- 这一步实际上把“collector -> web”的体验从多文件手动拼接，推进到了单文件导入。

验证：
- 继续跑 Codex adapter -> collector：
  - 输出成功生成：
    - `apps/collector/data/bundles/thr_demo.bundle.json`
- 生成的 `index.json` 已带有：
  - `bundle_path = bundles/thr_demo.bundle.json`
- 语法检查通过：
  - `python3 -m py_compile apps/collector/src/collector.py`
  - `node --check apps/web/app.js`

风险 / 未决问题：
- 当前 bundle 还是 per-session 文件，尚未提供多 session 的 bundle pack。
- web 虽然已能加载 bundle，但还没有更高级的 session picker、跨 session 比较和 bundle diff 能力。
- bundle 目前默认完整保留 event payload，未来可能还需要 redaction / export profile 设计。

下一步计划：
- 继续补更多真实 runtime fixture，扩展 Codex / OpenClaw 精度面
- 为 web 增加更强的 session picker 与 raw event inspector
- 设计 bundle redaction 策略和 multi-session pack 格式
